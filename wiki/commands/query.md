---
description: Answer a question from the wiki pages (not raw sources)
---

Answer this question from the wiki: **$ARGUMENTS**

Follow the `/query` rule in `CLAUDE.md`: synthesize from the wiki pages in `concepts/`, `entities/`, `decisions/` — do not re-read `raw/`. Cite the pages you used with `[[slug]]`. If the wiki can't answer it, say so plainly and suggest what to ingest.
